type Query {
  getworkshopemail(email: String): String!
}

type Mutation {
  workshop(
    name: String!
    email: String!
    classs: String!
    school: String!
    mobile: Int!
    area: String!
  ): [Error!]
}
